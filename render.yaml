services:
  - type: web
    name: allytdl
    runtime: node
    region: oregon
    buildCommand: npm run build
    startCommand: node dist/server/entry.mjs
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 22
